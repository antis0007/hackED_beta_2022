class Player {
    hand = []
    constructor(deck) {
        this.hand = deck
    }

    DrawCard(deck) {
        //Draws a card from the specified deck
        deck.DrawCard()
        this.hand.AddCard(hand)
    }

    PlayCard() {
        // 
    }

    Discard() {
        // Adds Card to Discard Pile
    }
    
}